apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aws-nth-bundle.clusterID" . }}-aws-node-termination-handler-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "aws-nth-bundle.labels" . | nindent 4 }}
data:
  values: |
  {{- (include "giantswarm.workloadValues" .) | nindent 4 }}
